<% if @location %>
    <h1> Location Address: <%= @location.street_address %> </h1>
<% else %>
    <h1> Please Sign In or Sign Up </h1>
<% end %>

<% if @measurements == [] %>
  <h5> This Location Has No Measurements </h5>
  <% else %>
<ul>
<% @measurements.each do |m|%>
  <li><%= m.employee.username %> - <%= m.length %>" x <%= m.width %>" - <%= link_to 'Edit', edit_location_measurement_path(@location, m.id)%> 
  </br>Measured On: <%= m.date %> </br></br>
<% end %>
</ul>
<% end %>

<p><%= link_to 'Add A New Measurement', new_location_measurement_path(@location)%></p>
